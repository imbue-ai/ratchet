[rule]
id = "no-while-true"
description = "'while True' loops can cause infinite loops. Use explicit conditions instead"
severity = "warning"

[match]
language = "python"
query = """
(while_statement
  condition: (true)) @violation
"""
